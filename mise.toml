[tools]
go = "1.23.0"
"github:lucasew/workspaced" = "0.3.4"

[tasks.codegen]
depends = ["codegen:*"]

[tasks.install]
depends = ["install:*"]

[tasks."install:go"]
run = "go mod download"

[tasks.test]
depends = ["test:*"]

[tasks."test:go"]
run = "go test ./..."

[tasks.lint]
run = "workspaced codebase lint"

[tasks.fmt]
run = "workspaced codebase format"

[tasks.ci]
depends = ["lint", "test"]
